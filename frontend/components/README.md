# Components

Reusable UI components for the mobile banking app.

## Structure

- **common/** - Shared components used throughout the app (buttons, inputs, cards, etc.)
- **screens/** - Screen-specific components
- **layout/** - Layout components (headers, footers, navigation)
